services:
  - type: web
    name: graham
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server/index.js
    autoDeploy: true
    envVars:
      # GRAHAM_MINT stays unset until $GRAHAM launches — set it in the Render dashboard
      # to light up the CA bar in the app.
      - key: EPOCH_SEC
        value: "28800"
